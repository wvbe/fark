#!/usr/bin/env node

'use strict';
const root = require('../src/ask');

root.execute(process.argv.slice(2))
	.then(result => {
		result.forEach(res => console.log(res));
		process.exit(0);
	})
	.catch(error => {
		console.log(error);
		process.exit(1);
	});
